/**
 * Day 29 - Algorithm Exercise
 * 
 * Problem: Coin Change
 */

export function coinChange(coins: number[], amount: number): number {
    // TODO: DP - min coins for each amount up to target
    return -1;
}

if (require.main === module) {
    console.log(coinChange([1, 2, 5], 11)); // 3
}


